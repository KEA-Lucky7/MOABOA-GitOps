apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "MOABOA-GitOps.fullname" . }}-test-connection"
  labels:
    {{- include "MOABOA-GitOps.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "MOABOA-GitOps.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
