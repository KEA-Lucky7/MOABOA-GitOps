apiVersion: v1
kind: Service
metadata:
  name: {{ include "MOABOA-GitOps.fullname" . }}
  labels:
    {{- include "MOABOA-GitOps.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "MOABOA-GitOps.selectorLabels" . | nindent 4 }}
